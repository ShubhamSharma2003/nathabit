# Normalize line endings: store LF in the repo and check out LF on every
# platform. Keeps diffs clean and matches the JS/TS ecosystem default (Prettier,
# ESLint), avoiding the "LF will be replaced by CRLF" churn on Windows.
* text=auto eol=lf
